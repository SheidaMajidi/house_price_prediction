[tool.poetry]
name = "house-price-prediction"
version = "0.1.0"
description = ""
authors = ["Sheida Majidi"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
